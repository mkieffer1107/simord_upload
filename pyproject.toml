[project]
name = "upload-simord"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets>=4.0.0",
    "huggingface-hub",
    "openai",
    "python-dotenv",
    "tiktoken",
]